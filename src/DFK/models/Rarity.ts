/**
 * Ids that are used for hero rarity. If we start using these in other places should probably be a more generic name.
 */
export const Rarity = {
  common: 0,
  uncommon: 1,
  rare: 2,
  legendary: 3,
  mythic: 4
} as const;
